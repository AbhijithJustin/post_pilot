class StartAutoModel {
  final bool isInstagramSelected;
  final bool isFacebookSelected;
  final bool isTwitterSelected;
  final bool isLinkedinSelected;

  StartAutoModel(
      {required this.isInstagramSelected,
      required this.isFacebookSelected,
      required this.isTwitterSelected,
      required this.isLinkedinSelected});
}
